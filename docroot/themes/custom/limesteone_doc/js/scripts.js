/**
 * @file
 * Behaviors for the limestone_doc theme.
 */

(function($, _, Drupal) {
  Drupal.behaviors.limestone_doc = {
    attach: function() {
      // limestone_doc JavaScript behaviors goes here.
    }
  };
})(window.jQuery, window._, window.Drupal);
